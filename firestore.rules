rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 사용자에게 읽기 권한 허용 (포트폴리오는 공개 데이터)
    // 쓰기는 불가 (Firebase Console에서만 수정 가능)
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}